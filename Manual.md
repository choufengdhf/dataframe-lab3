# 简易交互式计算器 — 用户使用手册

版本说明
- 这是一个基于命令行的交互式小程序，提供多项数据结构与数学运算功能：
  - 一元多项式（顺序表实现）
  - 一元多项式（链表实现）
  - 向量（顺序表实现）
  - 向量（链表实现）
  - 顺序表（基本线性表，整型）
  - 链表（基本线性表，整型）
  - 四则运算表达式求值（无变量数值运算）

目标读者
- 需要在命令行下进行多项式、向量等数据结构学习与基础运算的同学或开发者。
- 希望通过交互式菜单完成创建、运算、保存、删除等常见操作的用户。

目录
1. 环境与编译
2. 启动与退出
3. 主界面与菜单说明
4. 各模块操作指南（多项式 / 向量 / 线性表 / 四则运算）
5. 输入格式说明
6. 常见操作示例
7. 常见问题与排查建议
8. 进阶提示

---

1. 环境与编译
- 运行环境：Linux / macOS / WSL（Windows 子系统 Linux）；需安装 g++（建议支持 C++17）。
- 在项目根目录下编译（一次性编译所有源文件）：
```bash
rm -f calculator *.o
g++ -std=c++17 -Wall -Wextra -O2 *.cpp -o calculator
```
- 若使用 header-only 的 evaluator（表达式解析器），请不要同时编译有冲突的 evaluator.cpp，以免重复定义。
- 若需调试，使用带调试符号编译：
```bash
g++ -std=c++17 -g -O0 *.cpp -o calculator_dbg
gdb ./calculator_dbg
```

2. 启动与退出
- 启动程序：
```bash
./calculator
```
- 在任意菜单输入 `0` 可返回上一级或退出程序（在主菜单按 `0` 退出）。

3. 主界面与菜单说明
- 启动后显示一级菜单（选择模块）：
```
========= 简易计算器 =========
请选择模块:
1. 一元多项式（顺序表）
2. 一元多项式（链表）
3. 向量（顺序表）
4. 向量（链表）
5. 顺序表（基本线性表）
6. 链表（基本线性表）
7. 四则运算表达式求值（无变量）
0. 退出
请输入您的选择:
```
- 选择模块后进入对应二级菜单，按照提示输入编号以执行功能。

4. 各模块操作指南

A. 一元多项式（顺序表）
- 功能：创建、命名、列出、删除、加/减/乘、求导、在指定 x 点求值并可保存运算结果。
- 创建多项式：
  1. 选择“创建多项式并命名”。
  2. 输入名称（用于后续选择）。
  3. 输入表达式（例如 `2*x^3 + 4*x - 5`，支持括号与常整数次幂）。
  4. 系统解析并存储为命名多项式。
- 加/减/乘：
  - 系统会列出已保存的多项式（编号），按编号选择两个操作对象。
  - 计算并显示结果，提示是否保存为新多项式。
- 求导：
  - 选择待求导多项式，输入阶数（非负整数），显示并可保存结果。
- 赋值/求值：
  - 选择多项式并输入 x 的值（支持浮点），输出数值结果。

B. 一元多项式（链表）
- 功能同顺序表模块，内部实现不同（链表）。交互方式一致。

C. 向量（顺序表 / 链表）
- 功能：计算模长、向量点积、夹角余弦。
- 操作流程：
  1. 选择对应向量模块并功能项（如 1 模长）。
  2. 输入向量长度（正整数）。
  3. 依次输入每个分量（浮点或整数）。
  4. 系统输出结果；点积或夹角会要求输入两个向量。

D. 顺序表 / 链表（基本线性表，整型）
- 功能：创建、插入元素、遍历、清空、销毁。
- 操作示例：
  - 创建顺序表：输入元素个数并依次输入元素。
  - 插入：在提示处输入要插入的数值。
  - 遍历：显示当前所有元素。

E. 四则运算表达式求值（无变量）
- 输入一个纯数值表达式（支持 + - * / ^、小数点、括号）。
- 示例：`3 + 4 * 2.5`、`(1+2)^3 / 9`。
- 解析并输出结果（如果结果近似整数则以整数形式显示）。

5. 输入格式详细说明
- 多项式表达式（创建时）：
  - 支持项形式如 `ax^n`，例如 `2*x^3`、`-5*x`、`3`（常数项）。
  - 支持括号与幂运算，如 `(x+1)^2`（指数必须为整数常量）。
  - 不支持用除法产生多项式（表达式含 `/` 在解析为多项式时会报错）。
  - 变量名默认为 `x`。
- 向量输入：
  - 先输入长度 `n`（正整数），随后输入 `n` 个分量（每个回车或空格分隔均可，程序以提示逐项读取）。
- 菜单或编号选择：
  - 只输入编号并回车；若输入无效，程序会提示重新输入。
- 输入混合注意：
  - 程序同时使用了按键读取与整行读取（`cin >>` 与 `getline`），为避免残留换行影响，请按提示逐项回车或在出现问题时重新输入。推荐使用程序内的提示逐项输入。

6. 常见操作示例（完整交互片段）
- 创建并求值多项式（顺序表）：
```
请选择模块: 1
--- 一元多项式（顺序表）功能菜单 ---
请输入您的选择: 1
请输入多项式名称： p1
请输入多项式表达式（变量名默认为 x，例如：2*x^3 + 4*x - 5）:
2*x^3 + 4*x - 5
已创建多项式 'p1'。

回到二级菜单，选择 8（赋值/求值）：
请输入您的选择: 8
选择要赋值/求值的多项式：
  1. p1
输入编号选择（0 取消）: 1
请输入 x 的值（例如 2 或 3.5）： 2
多项式 p1 在 x=2 处的值为: 27
```

- 四则运算求值：
```
请选择模块: 7
请输入要计算的表达式（支持 + - * / ^ 和括号，支持浮点数，例如 3+4*2.5）:
(1+2)^3 / 9
结果: 3
```
---
